##alias one command
```
git config --global alias.<alias> <command>
```

##alias command containing more word
```
git config --global alias.<alias> "<command>"
```

##alias external command
```
git config --global alias.<alias> <!command>
````

